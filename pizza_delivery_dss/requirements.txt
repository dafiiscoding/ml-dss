# Pinned để deploy khớp môi trường tạo model (Python 3.12).
# Quan trọng: scikit-learn phải đúng 1.9.0 vì models/best_delay_model.joblib
# được pickle bằng phiên bản này; lệch version có thể không unpickle được.

# --- Runtime cho Streamlit app ---
streamlit==1.58.0
scikit-learn==1.9.0
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
plotly==6.8.0
openpyxl==3.1.5
joblib==1.5.3

# --- Dùng khi build artifact/figure (không bắt buộc cho app, giữ để tái lập) ---
matplotlib==3.11.0
seaborn==0.13.2

# --- Chỉ cần khi execute notebook (KHÔNG cần cho deploy app) ---
nbformat==5.10.4
nbclient==0.11.0
ipykernel==7.3.0
